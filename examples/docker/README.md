In this example, we use the module to generate a Quorum Network and runs it in Docker locally.

## Getting Started

```
terraform apply --auto-approve
```

`ethstats` is also available at http://localhost:3000